'''可能对比的几条消息中，同一个业务含义的字段，字段名称不一致，可在此进行关系映射，mapping_dict中的key需要和预期的dict中的key保持一致,且key一定要在value中的list中'''
# from src.Utils.Files.ReadExcel import ReadExcle
# from config.path_config import filed_mapping_path
# sample = ReadExcle(filed_mapping_path,"字段映射")
# mapping_dict = sample.get_field_mapping
mapping_dict = {}
