# This is a base config file which contains base_dir, driver path and so on!
import os

base_dir = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
conf_path = base_dir + r"\config\config.ini"
